# Claude Code Instructions

Use the `scientific-visualization` skill for publication figures, plots, scientific workflows, schematics, maps, graphical abstracts, journal-format conversion, and figure audits.

Canonical source:

```text
.agents/skills/scientific-visualization/
```

Install the project copy with:

```bash
python scripts/install.py --target claude --scope project --project .
```

Do not invent scientific content. Do not use generative-image output as data or observational evidence. Preserve vector masters, native 600-DPI archives, provenance, and manual QA.
