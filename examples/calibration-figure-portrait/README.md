# CalibrationFigurePortrait

Target profile: `nature`  
Orientation: `portrait`  
Final canvas: `89 x 160 mm`  
Archive raster: `600 DPI`

1. Replace the unresolved evidence contract in `figure_request.json`.
2. Add authoritative data without modifying the raw source.
3. Adapt `src/plot.py` and render vector plus 600-DPI archive outputs.
4. Update hashes and provenance in `figure_manifest.json`.
5. Run `figure_qc.py` and complete `qa/MANUAL_QA.md`.

The optional synthetic demo mode is only a rendering smoke test and labels itself accordingly.
