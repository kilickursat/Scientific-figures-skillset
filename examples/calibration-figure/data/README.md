# Data contract

Place authoritative source data in this directory. Do not overwrite raw evidence.

The default plotting template expects `observed,predicted` columns. Replace the template only after documenting the real variables, units, exclusions, missing-value policy, and uncertainty definition in `figure_request.json`.

Synthetic data are permitted only for a visibly labelled rendering-pipeline test and must never be presented as scientific evidence.
